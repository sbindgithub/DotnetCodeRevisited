## Distributed Caching

Unlike in memory caching that is stored in the memory of the web server, distributed caching is stored outside of the web server (separate caching server). This allows multiple web servers to share the same cache, which can improve performance and scalability.